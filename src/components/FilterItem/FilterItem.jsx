import styles from './FilterItem.module.scss'

const FilterItem = () => {
    return (
        <div className={styles.filter_item}>
            FilterItem
        </div>
    )
}

export default FilterItem